#!/usr/bin/env bash
# This script displays 12 hours and it's minutes
hour=0
minute=1
while [ $hours -le 12 ]; do
	echo "Hour: $hour"
  while [ $minutes -le 59 ]; do
	  echo "$minutes"
    
    minutes=$((minutes + 1))
  done
   hours=$((hours + 1))
  minutes=1
done
